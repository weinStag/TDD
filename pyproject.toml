[project]
name = "tdd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "coverage>=7.13.5",
    "mutmut<3",
    "pytest>=9.0.2",
]

[tool.mutmut]
paths_to_mutate = "ex07/pricing.py"
tests_dir = "ex07/"
